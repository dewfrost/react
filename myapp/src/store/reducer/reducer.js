import { combineReducers } from 'redux';

// 引入组件
//比如： import homebanner from './state/home/homebanner.js'

const reducer = combineReducers({
  // 各个组件中各自的状态
  // 获取值  store.getState().homebanner
  //比如： homebanner,
})

export default reducer;